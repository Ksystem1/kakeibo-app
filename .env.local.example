# プロジェクトルートにコピー: copy .env.local.example .env.local
# フロント再起動後に反映（Vite）
#
# 開発（npm run dev）では src/lib/api.ts が API を http://localhost:3456/api に固定（VITE_API_URL は無視）。
# キャッシュ疑い: npm run dev:web:force または npx vite --force
