node_modules
dist
# Cursor（共有ルールは .cursor/rules をコミット）
.cursor/*
!.cursor/rules/
!.cursor/rules/**
# Vite 設定は vite.config.ts のみ（.js との二重管理を防ぐ）
vite.config.js
*.local
.DS_Store
.env
backend/.env
# ドメイン・ARN メモ（infra/env.deploy.example をコピーしたもの）
infra/env.deploy.local
deploy-secrets.env
# Terraform（状態ファイルはコミットしない）
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.*
.terraform/
*.tfstate*
.terraform.lock.hcl
# RDS 資格情報（Terraform が Secrets Manager に書き込む値。コミット禁止）
infra/terraform-ecs-express/rds-credentials.auto.tfvars
