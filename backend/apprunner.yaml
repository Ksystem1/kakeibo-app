# App Runner: ソースディレクトリを backend にしたとき、このファイルが使われます。
# https://docs.aws.amazon.com/apprunner/latest/dg/service-source-code-nodejs.html
version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - npm ci
run:
  command: npm start
  network:
    port: 8080
